package;


class Main {

	var name = 'matthijs';
	// var name:String;

	public function setup() {
        // Serial.begin(115200);
		// var name = 'matthijs';

		// // trace('matthijs');
		// var arr = [1,2];
		// arr.push(3);
    }

    public function loop() {
		// // var isBool = true;
		// // trace(isBool);
		// var _string = 'a';
		// // var _bool = true;
        // Serial.println('Control Structure: if');
		// if (_string == 'a'){
		// 	trace(_string);
		// 	trace('a');
		// 	trace('$_string');

		// }
		// // if (isBool){
		// // 	trace('bool: true');
		// // }
		// // if (!isBool){
		// // 	trace('bool: false');
		// // }

        // Arduino.delay(1000);
    }

    static function main() {
    }
}